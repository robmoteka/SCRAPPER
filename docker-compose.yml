version: '3.8'

services:
  scrapper:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - MAX_DEPTH_LIMIT=5
      - DATA_DIR=/app/data
      - TIMEOUT=30
      - USER_AGENT=WebScraper/1.0
    restart: unless-stopped
